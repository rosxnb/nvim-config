# PATH FOR WINDOWS NVIM CONFIG FILE

This can be retrieved with command `echo stdpath("config")` from within NeoVim.


# Additional Dependencies

```
choco install lua-language-server
choco install zig
```

I had used `zig` to build the lsp servers but it turned out to be slow so decided to comment out lsp parts for now.
